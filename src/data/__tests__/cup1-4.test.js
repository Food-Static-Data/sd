/* global test, describe, it, expect */
'use strict'
import cup1-4 from '~/GroceriStar/cd/cup1-4.js'

describe('this test prevents to any issues and problems, also to break the structure of cuisines data cup1-4', () => {
  test('cup1-4 data files returns object', () => {
    expect(cup1-4).not.toBe('')
  })
})
