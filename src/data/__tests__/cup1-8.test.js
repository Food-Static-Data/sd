/* global test, describe, it, expect */
'use strict'
import cup1-8 from '~/GroceriStar/cd/cup1-8.js'

describe('this test prevents to any issues and problems, also to break the structure of cuisines data cup1-8', () => {
  test('cookng-times data files returns object', () => {
    expect(cup1-8).not.toBe('')
  })
})
