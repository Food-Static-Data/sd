/* global test, describe, it, expect */
'use strict'
import tablespoon-1 from '~/GroceriStar/cd/tablespoon-1.js'

describe('this test prevents to any issues and problems, also to break the structure of tablespoon-1 data', () => {
  test('tablespoon-1 data files returns object', () => {
    expect(tablespoon-1).not.toBe('')
  })
})
