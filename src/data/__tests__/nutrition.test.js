/* global test, describe, it, expect */
'use strict'
import nutritions1 from '~/Nutrition/nutritions1.json'

describe('nutritions1 data files returns array', () => {
  test('xxxx', () => {
    expect(nutritions1).not.toBe('')
  })
})
