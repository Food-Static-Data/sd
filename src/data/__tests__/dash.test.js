/* global test, describe, it, expect */
'use strict'
import dash from '~/GroceriStar/cd/dash.js'

describe('this test prevents to any issues and problems, also to break the structure of cuisines data dash', () => {
  test('cookng-times data files returns object', () => {
    expect(dash).not.toBe('')
  })
})
