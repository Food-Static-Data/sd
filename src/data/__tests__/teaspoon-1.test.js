/* global test, describe, it, expect */
'use strict'
import teaspoon-1 from '~/GroceriStar/cd/teaspoon-1.js'

describe('this test prevents to any issues and problems, also to break the structure of teaspoon-1 data', () => {
  test('teaspoon-1 data files returns object', () => {
    expect(teaspoon-1).not.toBe('')
  })
})
