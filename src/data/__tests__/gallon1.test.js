/* global test, describe, it, expect */
'use strict'
import gallon1 from '~/GroceriStar/cd/gallon1.js'

describe('this test prevents to any issues and problems, also to break the structure of cuisines data gallon1', () => {
  test('cookng-times data files returns object', () => {
    expect(gallon1).not.toBe('')
  })
})
