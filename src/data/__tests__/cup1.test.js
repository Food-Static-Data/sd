/* global test, describe, it, expect */
'use strict'
import cup1 from '~/GroceriStar/cd/cup1.js'

describe('this test prevents to any issues and problems, also to break the structure of cuisines data cup1', () => {
  test('cup1 data files returns object', () => {
    expect(cup1).not.toBe('')
  })
})
